import { OptsDirective } from './opts.directive';

describe('OptsDirective', () => {
  it('should create an instance', () => {
    const directive = new OptsDirective();
    expect(directive).toBeTruthy();
  });
});
